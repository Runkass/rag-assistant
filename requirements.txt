# Core runtime: API, DB, LLM client, chunking, config.
# Lightweight (~80 MB). Enough to run unit tests with mocked embeddings/reranker.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6

langchain-text-splitters>=0.3

psycopg[binary]>=3.2
pgvector>=0.3

openai>=1.55
httpx>=0.27

python-dotenv>=1.0

# Dev tools
pytest>=8.0
pytest-asyncio>=0.24
ruff>=0.7
